{
  "name": "mcp-perplexity-remote",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npx mcp-proxy --host 0.0.0.0 --port $PORT npx -y server-perplexity-ask"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "mcp-proxy": "5.9.0"
  }
}